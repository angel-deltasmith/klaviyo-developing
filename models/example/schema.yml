
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null


sources:
  - name: deltasmith
    database: klaviyo
    quoting:
      database: true
      schema: true
      identifier: true
      
    description: Sources from BigQuery data warehouse
    tables:
      - name: _airbyte_raw_klaviyo_campaigns
